import { Box, Typography } from '@mui/joy';

import React from 'react';

export interface FileUploadBoxProps {}

export default function FileUploadBox({}: FileUploadBoxProps) {
  return (
    <Box>
      <Typography>Hello, world!</Typography>
    </Box>
  );
}
